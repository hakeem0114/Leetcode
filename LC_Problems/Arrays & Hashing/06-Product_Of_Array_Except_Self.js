/**
 * @param {number[]} nums
 * @return {number[]}
 */
var productExceptSelf = function(nums) {
    let forwardArr  = []
    let start = 1 //Since we are multiplying, 1 should be the start val

    for(let i=0; i<nums.length;i++){
        
    }
    
};